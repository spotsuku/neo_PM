import { StubScreen } from "@/app/[orgSlug]/_stub";

export default function WbsPage() {
  return (
    <StubScreen
      emoji="📋"
      title="WBS / ガント"
      description="全タスクの階層・期間・進捗を1画面で。ガント / ツリー / カンバン切替。"
    />
  );
}

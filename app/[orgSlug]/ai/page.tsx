import { StubScreen } from "@/app/[orgSlug]/_stub";

export default function AIPage() {
  return (
    <StubScreen
      emoji="✨"
      title="AI 伴走者"
      description="フルスクリーンの AI 対話画面。提案カードを承認すると各画面に反映されます。"
    />
  );
}
